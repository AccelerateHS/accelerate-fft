# For more information, see: https://github.com/commercialhaskell/stack/blob/release/doc/yaml_configuration.md
# vim: nospell

resolver: lts-7.8

packages:
- '.'

# extra-deps:
- location:
    git:    https://github.com/AccelerateHS/accelerate.git
    commit: 362f7b80839dad39972f716f44e8c5cd56bd3c87
  extra-dep: true

- location:
    git:    https://github.com/AccelerateHS/accelerate-cuda.git
    commit: 3ff507ce7e76312187a18ccd78d25679620c9d59
  extra-dep: true

- location:
    git:    https://github.com/AccelerateHS/accelerate-llvm.git
    commit: 932e4ff0cb7d77fccd6d44580646cec9fb8d0083
  extra-dep: true
  subdirs:
    - 'accelerate-llvm'
    - 'accelerate-llvm-native'
    - 'accelerate-llvm-ptx'

- location:
    git:    https://github.com/robeverest/cufft.git
    commit: ca8183acb639c43715c6581af82ee246cee6a46f
  extra-dep: true

- location:
    git:    https://github.com/bscarlet/llvm-general.git
    # commit: ec1ad5bd2112e7f64dbb43441b5e2075cf6ad8e7  # 3.9
    commit: 529f9bf117b5ffd1ccf7ebeb679a97b3b1c67938  # 3.8
  extra-dep: true
  subdirs:
    - 'llvm-general'
    - 'llvm-general-pure'

extra-deps:
- 'chaselev-deque-0.5.0.5'
- 'cuda-0.7.5.1'
- 'libffi-0.1'
- 'nvvm-0.7.5.1'
- 'unique-0'

# Override default flag values for local packages and extra-deps
flags:
  llvm-general:
    shared-llvm: true
  accelerate:
    unsafe-checks: false
    bounds-checks: false
    debug: false
    internal-checks: false
  accelerate-cuda:
    debug: false
  accelerate-fft:
    cuda: true
    llvm-cpu: true
    llvm-ptx: true

# Extra package databases containing global packages
extra-package-dbs: []

# Control whether we use the GHC we find on the path
# system-ghc: true

# Require a specific version of stack, using version ranges
# require-stack-version: -any # Default
# require-stack-version: >= 0.1.4.0

# Override the architecture used by stack, especially useful on Windows
# arch: i386
# arch: x86_64

# Extra directories used by stack for building
# extra-include-dirs: [/path/to/dir]
# extra-lib-dirs: [/path/to/dir]

