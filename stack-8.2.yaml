# For more information, see: https://github.com/commercialhaskell/stack/blob/release/doc/yaml_configuration.md
# vim: nospell

resolver: nightly-2017-11-01

packages:
- '.'

- location:
    git:    https://github.com/tmcdonell/accelerate.git
    commit: 216538e42140a70fb8f5e4d3c0f045c33e5babfe
  extra-dep: true

- location:
    git:    https://github.com/tmcdonell/accelerate-llvm.git
    commit: a329e73cf0ba8413ecb961c3eda9e0c07aee2650
  extra-dep: true
  subdirs:
    - accelerate-llvm
    - accelerate-llvm-native
    - accelerate-llvm-ptx

- location:
    git:    https://github.com/tmcdonell/cuda.git
    commit: 4272d6e2fdcfefdd6798d81c7af8aff6dc050b1a
  extra-dep: true

extra-deps: []

# Override default flag values for local packages and extra-deps
flags: {}

# Extra package databases containing global packages
extra-package-dbs: []

# Control whether we use the GHC we find on the path
# system-ghc: true

# Require a specific version of stack, using version ranges
# require-stack-version: -any # Default
# require-stack-version: >= 0.1.4.0

# Override the architecture used by stack, especially useful on Windows
# arch: i386
# arch: x86_64

# Extra directories used by stack for building
# extra-include-dirs: [/path/to/dir]
# extra-lib-dirs: [/path/to/dir]

